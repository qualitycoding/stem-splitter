Test sources are written in S-001..S-005 against the frozen list in `plan/PLAN.md`; `FROZEN_MANIFEST.sha256` records a SHA-256 for every file under `tests/` (except `PERF.md`); CI runs `npm run verify:frozen` and fails if any test or fixture changed without the manifest being regenerated (`node scripts/frozen-manifest.mjs --write`, reviewed in the same diff). It was first written 2026-09-25, after S-007, not at S-001 as planned; the T-006 fixture was revised just before (research/spikes/SP-3.md).

The Vitest browser server is not cross-origin isolated by default, so browser tests run single-threaded WASM (which doubles as the T-023 slow-mode check). Set `VITE_TEST_ISOLATE=1` for COOP/COEP + multi-threaded WASM; the opt-in golden-parity/performance workflow does. Playwright WebKit on Windows has no `OfflineAudioContext`, so `decode`/`separate` browser tests fail there locally; they pass on Linux CI.
