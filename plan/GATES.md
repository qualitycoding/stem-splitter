# Gates

- **G-001:** N/A — no headline mathematical or computational claim.
- **G-002 Deployment.** Trigger: S-008, before the workflow file is committed to `.github/workflows/`. Evidence: local `npm run build` + full test run green, `tests/PERF.md`, `plan/OPERATIONS.md`, `plan/templates/deploy.yml`. Question: "Enable GitHub Pages deployment?" Responses: `proceed` | `proceed-with-rescope: <text>` | `stop`.
